ollama:
  enabled: true
  ollama:
    gpu:
      enabled: false
      type: 'nvidia'
      number: 1
  podLabels:
    istio.io/dataplane-mode: ambient
  resources:
    requests:
      cpu: 10000m
      memory: 9Gi
    limits:
      memory: 9Gi
  persistentVolume:
    enabled: true
    size: 30Gi
    storageClass: cstor-csi

pipelines:
  enabled: true
  persistence:
    enabled: true
    size: 2Gi
    storageClass: cstor-csi
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
    limits:
      memory: 100Mi


podLabels:
  istio.io/dataplane-mode: ambient

resources:
  requests:
    cpu: 10m
    memory: 800Mi
  limits:
    memory: 800Mi

persistence:
  enabled: true
  size: 2Gi
  storageClass: cstor-csi
