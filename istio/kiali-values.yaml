cr:
  create: true
  namespace: istio-system

  spec:
    external_services:
      istio:
        component_status:
          components:
          - app_label: "istiod"
            is_core: true
            is_proxy: false
          - app_label: "istio-ingress"
            is_core: true
            is_proxy: true
            namespace: istio-ingress
