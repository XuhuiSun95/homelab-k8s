apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  name: karpenter-provider-proxmox
  namespace: argocd
spec:
  project: default
  destination:
    namespace: kube-system
    server: 'https://kubernetes.default.svc'
  sources:
    - path: . 
      repoURL: 'oci://ghcr.io/sergelogvinov/charts/karpenter-provider-proxmox'
      targetRevision: 0.4.2
      helm:
        valueFiles:
        - $values/terraform/files/proxmox-karpenter.yaml
    - repoURL: 'https://github.com/XuhuiSun95/homelab-k8s.git'
      targetRevision: main
      ref: values
  # syncPolicy:
  #   automated:
  #     selfHeal: true