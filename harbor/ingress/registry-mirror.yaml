apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: registry-mirror-vs
  namespace: harbor
spec:
  hosts:
  - registry-mirror.local.xuhuisun.com
  gateways:
  - istio-ingress/gateway
  http:
  - route:
    - destination:
        host: harbor
        port:
          number: 80
